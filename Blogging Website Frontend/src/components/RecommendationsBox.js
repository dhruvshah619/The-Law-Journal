import React,{useState} from 'react'
import '../styling/RecommendationsBox.css';

function RecommendationsBox() {
    return (
        <div>
            {/* Iterate through the list of recommendations obtained and call each of them with values as providing it with the UserCard component*/}
        </div>
    )
}

export default RecommendationsBox
